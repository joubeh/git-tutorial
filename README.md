# what we have learned

- `git init`
- `git clone <url>`
- `git add <file>`
- `git commit -m <commit details>`
- `git push`
- `git pull`
- `git log`
- `git checkout <commit id>`
- `git branch <branch name>`
- `git merge`
